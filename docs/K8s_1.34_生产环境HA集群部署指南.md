# Kubernetes 1.34 生产环境高可用集群部署指南

> 3 Master + 3 External etcd + HAProxy/Keepalived · Ubuntu 22.04 · kubeadm + containerd
>
> 撰写人：孟希東

---

## 架构设计

### 节点规划

| 角色 | 主机名 | IP | 配置 | 备注 |
|------|--------|-----|------|------|
| etcd-01 + HAProxy + Keepalived(MASTER) | etcd-01 | 10.0.0.11 | 2C/4G/50G SSD | etcd 数据盘建议独立 SSD |
| etcd-02 + HAProxy + Keepalived(BACKUP) | etcd-02 | 10.0.0.12 | 2C/4G/50G SSD | |
| etcd-03 | etcd-03 | 10.0.0.13 | 2C/4G/50G SSD | |
| Control Plane 1 | k8s-master-01 | 10.0.0.21 | 4C/8G/100G | |
| Control Plane 2 | k8s-master-02 | 10.0.0.22 | 4C/8G/100G | |
| Control Plane 3 | k8s-master-03 | 10.0.0.23 | 4C/8G/100G | |
| Worker (GPU) 1 | k8s-gpu-01 | 10.0.0.31 | 8C/32G/200G+GPU | |
| Worker (GPU) 2 | k8s-gpu-02 | 10.0.0.32 | 8C/32G/200G+GPU | |
| Worker (GPU) 3 | k8s-gpu-03 | 10.0.0.33 | 8C/32G/200G+GPU | |
| **VIP** | — | **10.0.0.100** | — | Keepalived 虚拟 IP |

### 网络规划

| 用途 | CIDR |
|------|------|
| 节点网络 | 10.0.0.0/24 |
| Pod 网络 | 192.168.0.0/16（Calico 默认） |
| Service 网络 | 10.96.0.0/12（K8s 默认） |
| API Server VIP | 10.0.0.100:6443 |
| etcd 集群 | 10.0.0.11-13:2379（客户端），2380（peer） |

### 端口要求

| 节点 | 端口 | 用途 |
|------|------|------|
| etcd | 2379, 2380 | 客户端通信, peer 通信 |
| Master | 6443, 2379-2380, 10250, 10259, 10257 | API Server, kubelet 等 |
| Worker | 10250, 30000-32767 | kubelet, NodePort |
| LB | 6443 | HAProxy 转发到 API Server |

---

## Phase 1 · 所有节点基础准备（9 台全部执行）

### 1.1 设置主机名和 hosts

```bash
# 各节点分别设置对应主机名
sudo hostnamectl set-hostname <对应主机名>

# 所有节点统一 hosts
cat <<EOF | sudo tee -a /etc/hosts
10.0.0.11  etcd-01
10.0.0.12  etcd-02
10.0.0.13  etcd-03
10.0.0.21  k8s-master-01
10.0.0.22  k8s-master-02
10.0.0.23  k8s-master-03
10.0.0.31  k8s-gpu-01
10.0.0.32  k8s-gpu-02
10.0.0.33  k8s-gpu-03
10.0.0.100 k8s-api-vip
EOF
```

### 1.2 关闭 Swap

```bash
sudo swapoff -a
sudo sed -i '/ swap / s/^\(.*\)$/#\1/g' /etc/fstab
```

### 1.3 加载内核模块和参数

```bash
cat <<EOF | sudo tee /etc/modules-load.d/k8s.conf
overlay
br_netfilter
EOF

sudo modprobe overlay
sudo modprobe br_netfilter

cat <<EOF | sudo tee /etc/sysctl.d/k8s.conf
net.bridge.bridge-nf-call-iptables  = 1
net.bridge.bridge-nf-call-ip6tables = 1
net.ipv4.ip_forward                 = 1
EOF

sudo sysctl --system
```

### 1.4 关闭防火墙

```bash
sudo systemctl disable --now ufw
```

### 1.5 时间同步

```bash
sudo apt-get install -y chrony
sudo systemctl enable --now chrony
chronyc tracking   # 确认同步
```

> ⚠️ 生产环境必须保证所有节点时间一致，etcd 对时钟偏差非常敏感（>500ms 会选举失败）。

---

## Phase 2 · 生成 TLS 证书（在任意一台机器操作）

> etcd 集群通信和客户端连接必须使用 TLS。建议在 etcd-01 上操作，然后分发。

### 2.1 安装 cfssl 工具

```bash
sudo apt-get install -y golang-cfssl

# 或手动下载
# curl -sL https://github.com/cloudflare/cfssl/releases/download/v1.6.5/cfssl_1.6.5_linux_amd64 -o /usr/local/bin/cfssl
# curl -sL https://github.com/cloudflare/cfssl/releases/download/v1.6.5/cfssljson_1.6.5_linux_amd64 -o /usr/local/bin/cfssljson
# chmod +x /usr/local/bin/cfssl /usr/local/bin/cfssljson
```

### 2.2 生成 CA 证书

```bash
mkdir -p ~/etcd-certs && cd ~/etcd-certs

cat > ca-config.json <<EOF
{
  "signing": {
    "default": { "expiry": "87600h" },
    "profiles": {
      "etcd": {
        "usages": ["signing", "key encipherment", "server auth", "client auth"],
        "expiry": "87600h"
      }
    }
  }
}
EOF

cat > ca-csr.json <<EOF
{
  "CN": "etcd-ca",
  "key": { "algo": "rsa", "size": 2048 },
  "names": [{ "O": "etcd", "OU": "CA" }]
}
EOF

cfssl gencert -initca ca-csr.json | cfssljson -bare ca
# 生成: ca.pem  ca-key.pem  ca.csr
```

### 2.3 生成 etcd 服务端证书

```bash
cat > etcd-csr.json <<EOF
{
  "CN": "etcd",
  "hosts": [
    "127.0.0.1",
    "10.0.0.11",
    "10.0.0.12",
    "10.0.0.13",
    "etcd-01",
    "etcd-02",
    "etcd-03"
  ],
  "key": { "algo": "rsa", "size": 2048 },
  "names": [{ "O": "etcd", "OU": "etcd cluster" }]
}
EOF

cfssl gencert -ca=ca.pem -ca-key=ca-key.pem -config=ca-config.json -profile=etcd etcd-csr.json | cfssljson -bare etcd
# 生成: etcd.pem  etcd-key.pem
```

### 2.4 分发证书到 3 台 etcd 节点

```bash
ETCD_NODES="etcd-01 etcd-02 etcd-03"
for node in $ETCD_NODES; do
  ssh $node "sudo mkdir -p /etc/etcd/ssl"
  scp ca.pem etcd.pem etcd-key.pem $node:/tmp/
  ssh $node "sudo mv /tmp/{ca.pem,etcd.pem,etcd-key.pem} /etc/etcd/ssl/ && sudo chmod 600 /etc/etcd/ssl/*-key.pem"
done
```

### 2.5 分发证书到 3 台 Master 节点

```bash
# Master 节点需要 etcd CA 和客户端证书来连接 etcd
MASTER_NODES="k8s-master-01 k8s-master-02 k8s-master-03"
for node in $MASTER_NODES; do
  ssh $node "sudo mkdir -p /etc/kubernetes/pki/etcd"
  scp ca.pem $node:/tmp/etcd-ca.pem
  scp etcd.pem $node:/tmp/apiserver-etcd-client.pem
  scp etcd-key.pem $node:/tmp/apiserver-etcd-client-key.pem
  ssh $node "sudo mv /tmp/etcd-ca.pem /etc/kubernetes/pki/etcd/ca.crt && sudo mv /tmp/apiserver-etcd-client.pem /etc/kubernetes/pki/apiserver-etcd-client.crt && sudo mv /tmp/apiserver-etcd-client-key.pem /etc/kubernetes/pki/apiserver-etcd-client.key"
done
```

---

## Phase 3 · 部署 etcd 集群（3 台 etcd 节点）

### 3.1 下载 etcd 二进制

```bash
ETCD_VER=v3.5.17

curl -sL https://github.com/etcd-io/etcd/releases/download/${ETCD_VER}/etcd-${ETCD_VER}-linux-amd64.tar.gz | tar xz
sudo mv etcd-${ETCD_VER}-linux-amd64/etcd* /usr/local/bin/

# 验证
etcd --version
etcdctl version
```

### 3.2 创建 etcd systemd 服务

在**每台 etcd 节点**上创建服务文件，注意替换 `THIS_IP` 和 `THIS_NAME`：

```bash
# 设置当前节点变量（每台节点不同！）
THIS_NAME=etcd-01        # etcd-01 / etcd-02 / etcd-03
THIS_IP=10.0.0.11        # 10.0.0.11 / 10.0.0.12 / 10.0.0.13

sudo mkdir -p /var/lib/etcd

cat <<EOF | sudo tee /etc/systemd/system/etcd.service
[Unit]
Description=etcd
After=network.target

[Service]
Type=notify
ExecStart=/usr/local/bin/etcd \\
  --name=${THIS_NAME} \\
  --data-dir=/var/lib/etcd \\
  --listen-client-urls=https://${THIS_IP}:2379,https://127.0.0.1:2379 \\
  --advertise-client-urls=https://${THIS_IP}:2379 \\
  --listen-peer-urls=https://${THIS_IP}:2380 \\
  --initial-advertise-peer-urls=https://${THIS_IP}:2380 \\
  --initial-cluster=etcd-01=https://10.0.0.11:2380,etcd-02=https://10.0.0.12:2380,etcd-03=https://10.0.0.13:2380 \\
  --initial-cluster-token=etcd-cluster-prod \\
  --initial-cluster-state=new \\
  --client-cert-auth=true \\
  --trusted-ca-file=/etc/etcd/ssl/ca.pem \\
  --cert-file=/etc/etcd/ssl/etcd.pem \\
  --key-file=/etc/etcd/ssl/etcd-key.pem \\
  --peer-client-cert-auth=true \\
  --peer-trusted-ca-file=/etc/etcd/ssl/ca.pem \\
  --peer-cert-file=/etc/etcd/ssl/etcd.pem \\
  --peer-key-file=/etc/etcd/ssl/etcd-key.pem
Restart=always
RestartSec=5
LimitNOFILE=65536

[Install]
WantedBy=multi-user.target
EOF
```

### 3.3 启动 etcd（3 台同时启动）

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now etcd
```

> ⚠️ 3 台必须在短时间内全部启动，否则集群无法选举。如果某台启动超时，检查防火墙和证书。

### 3.4 验证 etcd 集群

```bash
ETCDCTL_API=3 etcdctl \
  --cacert=/etc/etcd/ssl/ca.pem \
  --cert=/etc/etcd/ssl/etcd.pem \
  --key=/etc/etcd/ssl/etcd-key.pem \
  --endpoints=https://10.0.0.11:2379,https://10.0.0.12:2379,https://10.0.0.13:2379 \
  endpoint health

# 期望输出：3 个 healthy
# https://10.0.0.11:2379 is healthy
# https://10.0.0.12:2379 is healthy
# https://10.0.0.13:2379 is healthy

# 查看集群成员
ETCDCTL_API=3 etcdctl \
  --cacert=/etc/etcd/ssl/ca.pem \
  --cert=/etc/etcd/ssl/etcd.pem \
  --key=/etc/etcd/ssl/etcd-key.pem \
  --endpoints=https://10.0.0.11:2379 \
  member list -w table
```

---

## Phase 4 · 部署 HAProxy + Keepalived（etcd-01 和 etcd-02）

### 4.1 安装

```bash
# 在 etcd-01 和 etcd-02 上执行
sudo apt-get install -y haproxy keepalived
```

### 4.2 配置 HAProxy

```bash
# 两台 HAProxy 配置相同
cat <<EOF | sudo tee /etc/haproxy/haproxy.cfg
global
    log /dev/log local0
    maxconn 4096
    daemon

defaults
    log     global
    mode    tcp
    option  tcplog
    option  dontlognull
    timeout connect 5s
    timeout client  30s
    timeout server  30s
    retries 3

frontend k8s-api
    bind *:6443
    default_backend k8s-api-backend

backend k8s-api-backend
    option httpchk GET /healthz
    http-check expect status 200
    balance roundrobin
    server master-01 10.0.0.21:6443 check check-ssl verify none
    server master-02 10.0.0.22:6443 check check-ssl verify none
    server master-03 10.0.0.23:6443 check check-ssl verify none

listen stats
    bind *:9090
    mode http
    stats enable
    stats uri /stats
    stats auth admin:admin123
EOF

sudo systemctl enable --now haproxy
sudo systemctl status haproxy
```

### 4.3 配置 Keepalived

**etcd-01（MASTER）：**

```bash
cat <<EOF | sudo tee /etc/keepalived/keepalived.conf
vrrp_script check_haproxy {
    script "/usr/bin/killall -0 haproxy"
    interval 2
    weight -20
    fall 3
    rise 2
}

vrrp_instance VI_K8S {
    state MASTER
    interface eth0
    virtual_router_id 51
    priority 100
    advert_int 1
    authentication {
        auth_type PASS
        auth_pass K8sHA@2026
    }
    virtual_ipaddress {
        10.0.0.100/24
    }
    track_script {
        check_haproxy
    }
}
EOF
```

**etcd-02（BACKUP）：**

```bash
cat <<EOF | sudo tee /etc/keepalived/keepalived.conf
vrrp_script check_haproxy {
    script "/usr/bin/killall -0 haproxy"
    interval 2
    weight -20
    fall 3
    rise 2
}

vrrp_instance VI_K8S {
    state BACKUP
    interface eth0
    virtual_router_id 51
    priority 90
    advert_int 1
    authentication {
        auth_type PASS
        auth_pass K8sHA@2026
    }
    virtual_ipaddress {
        10.0.0.100/24
    }
    track_script {
        check_haproxy
    }
}
EOF
```

> ⚠️ `interface eth0` 需替换为实际网卡名（`ip a` 查看）。

### 4.4 启动并验证

```bash
sudo systemctl enable --now keepalived

# 在 etcd-01 上检查 VIP
ip a show eth0 | grep 10.0.0.100
# 应看到 10.0.0.100/24

# 从任意节点测试 VIP 可达
ping -c 3 10.0.0.100
```

---

## Phase 5 · 安装 containerd（6 台：3 Master + 3 Worker）

> etcd 节点不需要安装 containerd。

```bash
# 安装 containerd
sudo apt-get update
sudo apt-get install -y ca-certificates curl gnupg
sudo install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list
sudo apt-get update
sudo apt-get install -y containerd.io

# 配置 SystemdCgroup
sudo mkdir -p /etc/containerd
containerd config default | sudo tee /etc/containerd/config.toml
sudo sed -i 's/SystemdCgroup = false/SystemdCgroup = true/' /etc/containerd/config.toml
sudo systemctl restart containerd
sudo systemctl enable containerd
```

---

## Phase 6 · 安装 kubeadm/kubelet/kubectl（6 台：3 Master + 3 Worker）

```bash
sudo apt-get install -y apt-transport-https ca-certificates curl gpg
curl -fsSL https://pkgs.k8s.io/core:/stable:/v1.34/deb/Release.key | sudo gpg --dearmor -o /etc/apt/keyrings/kubernetes-apt-keyring.gpg
echo 'deb [signed-by=/etc/apt/keyrings/kubernetes-apt-keyring.gpg] https://pkgs.k8s.io/core:/stable:/v1.34/deb/ /' | sudo tee /etc/apt/sources.list.d/kubernetes.list
sudo apt-get update
sudo apt-get install -y kubelet kubeadm kubectl
sudo apt-mark hold kubelet kubeadm kubectl
sudo systemctl enable kubelet
```

---

## Phase 7 · 初始化第一个 Master（k8s-master-01）

### 7.1 创建 kubeadm 配置文件

```bash
cat <<EOF > kubeadm-config.yaml
apiVersion: kubeadm.k8s.io/v1beta4
kind: ClusterConfiguration
kubernetesVersion: "v1.34.0"
controlPlaneEndpoint: "10.0.0.100:6443"
networking:
  podSubnet: "192.168.0.0/16"
  serviceSubnet: "10.96.0.0/12"
etcd:
  external:
    endpoints:
      - https://10.0.0.11:2379
      - https://10.0.0.12:2379
      - https://10.0.0.13:2379
    caFile: /etc/kubernetes/pki/etcd/ca.crt
    certFile: /etc/kubernetes/pki/apiserver-etcd-client.crt
    keyFile: /etc/kubernetes/pki/apiserver-etcd-client.key
---
apiVersion: kubeadm.k8s.io/v1beta4
kind: InitConfiguration
localAPIEndpoint:
  advertiseAddress: "10.0.0.21"
  bindPort: 6443
EOF
```

### 7.2 执行初始化

```bash
sudo kubeadm init --config=kubeadm-config.yaml --upload-certs
```

### 7.3 配置 kubectl

```bash
mkdir -p $HOME/.kube
sudo cp -i /etc/kubernetes/admin.conf $HOME/.kube/config
sudo chown $(id -u):$(id -g) $HOME/.kube/config
```

### 7.4 保存输出的 join 命令

init 完成后会输出两条 join 命令，分别用于 Master 和 Worker：

```
# Master 节点加入（带 --control-plane --certificate-key）
kubeadm join 10.0.0.100:6443 --token <token> \
  --discovery-token-ca-cert-hash sha256:<hash> \
  --control-plane --certificate-key <cert-key>

# Worker 节点加入
kubeadm join 10.0.0.100:6443 --token <token> \
  --discovery-token-ca-cert-hash sha256:<hash>
```

---

## Phase 8 · 加入其余 Master 节点（k8s-master-02/03）

### 8.1 复制 etcd 客户端证书（Phase 2.5 已完成）

确认 `/etc/kubernetes/pki/etcd/` 下有 ca.crt、apiserver-etcd-client.crt、apiserver-etcd-client.key。

### 8.2 执行 join

```bash
# 在 k8s-master-02 和 k8s-master-03 上分别执行
sudo kubeadm join 10.0.0.100:6443 --token <token> \
  --discovery-token-ca-cert-hash sha256:<hash> \
  --control-plane --certificate-key <cert-key>

# 配置 kubectl
mkdir -p $HOME/.kube
sudo cp -i /etc/kubernetes/admin.conf $HOME/.kube/config
sudo chown $(id -u):$(id -g) $HOME/.kube/config
```

### 8.3 验证 3 Master

```bash
kubectl get nodes
# NAME             STATUS     ROLES           VERSION
# k8s-master-01    NotReady   control-plane   v1.34.0
# k8s-master-02    NotReady   control-plane   v1.34.0
# k8s-master-03    NotReady   control-plane   v1.34.0
```

> NotReady 是正常的，等安装 CNI 后变为 Ready。

---

## Phase 9 · 安装 Calico CNI（任一 Master 执行）

```bash
kubectl create -f https://raw.githubusercontent.com/projectcalico/calico/v3.29.0/manifests/tigera-operator.yaml
kubectl create -f https://raw.githubusercontent.com/projectcalico/calico/v3.29.0/manifests/custom-resources.yaml

# 等待所有 Calico Pod Running
watch kubectl get pods -n calico-system

# 确认所有 Master 变为 Ready
kubectl get nodes
```

---

## Phase 10 · 加入 Worker 节点（3 台 GPU 节点）

```bash
# 在每台 Worker 上执行
sudo kubeadm join 10.0.0.100:6443 --token <token> \
  --discovery-token-ca-cert-hash sha256:<hash>
```

在 Master 上验证：

```bash
kubectl get nodes -o wide
# 6 个节点全部 Ready

# 打标签
kubectl label node k8s-gpu-01 node-role.kubernetes.io/worker=worker gpu=true
kubectl label node k8s-gpu-02 node-role.kubernetes.io/worker=worker gpu=true
kubectl label node k8s-gpu-03 node-role.kubernetes.io/worker=worker gpu=true
```

---

## Phase 11 · 安装 Helm

```bash
curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash
helm repo add nvidia https://helm.ngc.nvidia.com/nvidia
helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
helm repo update
helm version
```

---

## Phase 12 · 集群验证

### 12.1 完整健康检查

```bash
# 节点状态
kubectl get nodes -o wide

# 系统 Pod
kubectl get pods -n kube-system -o wide
kubectl get pods -n calico-system

# etcd 健康（从 Master 执行）
ETCDCTL_API=3 etcdctl \
  --cacert=/etc/kubernetes/pki/etcd/ca.crt \
  --cert=/etc/kubernetes/pki/apiserver-etcd-client.crt \
  --key=/etc/kubernetes/pki/apiserver-etcd-client.key \
  --endpoints=https://10.0.0.11:2379,https://10.0.0.12:2379,https://10.0.0.13:2379 \
  endpoint health
```

### 12.2 HA 故障切换测试

```bash
# 测试 1：关闭 Master-01，验证集群仍可用
ssh k8s-master-01 "sudo systemctl stop kubelet"
kubectl get nodes          # Master-01 NotReady，集群正常
kubectl create deployment test --image=nginx --replicas=3
kubectl get pods -o wide   # Pod 调度到其他节点

# 测试 2：关闭 HAProxy MASTER，验证 VIP 漂移
ssh etcd-01 "sudo systemctl stop haproxy"
ping 10.0.0.100           # 仍可达（VIP 漂移到 etcd-02）
kubectl get nodes          # 通过 VIP 仍能访问

# 恢复
ssh k8s-master-01 "sudo systemctl start kubelet"
ssh etcd-01 "sudo systemctl start haproxy"
kubectl delete deployment test
```

---

## 运维要点

### etcd 备份（必须配置！）

```bash
# 手动备份
ETCDCTL_API=3 etcdctl snapshot save /backup/etcd-$(date +%Y%m%d).db \
  --cacert=/etc/etcd/ssl/ca.pem \
  --cert=/etc/etcd/ssl/etcd.pem \
  --key=/etc/etcd/ssl/etcd-key.pem \
  --endpoints=https://10.0.0.11:2379

# 建议 crontab 每小时备份
0 * * * * /usr/local/bin/etcdctl snapshot save /backup/etcd-$(date +\%Y\%m\%d-\%H).db --cacert=... --cert=... --key=... --endpoints=... 2>&1 | logger -t etcd-backup
```

### 证书续期

- etcd 证书有效期 10 年（cfssl 配置的 87600h）
- K8s 组件证书默认 1 年，`kubeadm certs renew all` 续期
- 建议设置提醒：`kubeadm certs check-expiration`

### 监控建议

- HAProxy stats：http://10.0.0.11:9090/stats（admin/admin123）
- etcd metrics：各节点 :2379/metrics
- K8s：部署 Prometheus + Grafana（通过 Helm）
